server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # โหลด mime.types มาตรฐาน
  include /etc/nginx/mime.types;
  default_type application/octet-stream;

  # ให้ Angular SPA ทำงานได้เวลา refresh หน้า (เช่น /register, /login)
  location / {
    try_files $uri $uri/ /index.html;
  }

  # ให้ไฟล์ .js เสิร์ฟเป็น application/javascript
  types {
    text/javascript js;
    application/wasm wasm;
  }

  # Proxy ไป Backend (แก้ชื่อ service ให้ตรงกับ K8s ของคุณ)
  location /todo-list-service/ {
    proxy_pass http://todo-backend-svc:8080/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # WebSocket support (ถ้ามี)
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
  }
}
